services:
  - type: web
    name: sm-report-backend
    env: python
    rootDirectory: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    pythonVersion: 3.12.2